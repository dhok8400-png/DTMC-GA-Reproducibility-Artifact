Phase 3A executable demo implementation.

The modules here implement an end-to-end pipeline for a small CSV-based benchmark. The current reader is not a Clang/LLVM parser; it is a normalized input layer used to validate the computational pipeline before full benchmark instrumentation.

# Benchmarks

This folder stores benchmark metadata and instructions. Do not store third-party source code here unless its license allows redistribution.

Required metadata columns are defined in `benchmark_metadata.csv`.

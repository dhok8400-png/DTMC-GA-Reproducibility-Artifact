# cyclic_workflow_20

Phase 3B executable CSV benchmark fixture.

This benchmark is generated inside the artifact and is used to produce reproducible raw results for the executable pipeline. It is not a third-party project checkout and must not be presented as Chromium/Firefox/MySQL evidence.

Subsystem model: workflow_core_subsystem
Description: workflow benchmark with bounded cyclic dependencies and guarded asynchronous edges
Classes: 20
